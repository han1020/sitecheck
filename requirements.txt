# 2026-08-31 서버 pip freeze 기준으로 버전 고정 (== 고정: 캐시 재빌드 시 조용한 업그레이드 방지)
# torch/torchvision은 CPU 휠 고정을 위해 Dockerfile에서 별도 설치 (torch==2.13.0+cpu, torchvision==0.28.0+cpu)
playwright==1.62.0
openpyxl==3.1.5
PyYAML==6.0.3
APScheduler==3.11.3
python-dateutil==2.9.0.post0
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.2
pycryptodome==3.23.0
easyocr==1.7.2        # 이미지 공지 OCR(선택). 미설치 시 해당 공지는 '검토 필요'로 남음
